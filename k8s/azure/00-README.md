You will:
1) Build+push images to ACR.
2) Create Kubernetes Secrets with your real connection strings/keys.
3) Apply manifests.
4) Point DNS (splitttr.com + api.splitttr.com) to the Ingress public IP.
